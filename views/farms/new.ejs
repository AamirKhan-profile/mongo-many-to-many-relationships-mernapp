<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Farm</title>

    <!-- Bootstrap 5 CSS -->
    <link 
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" 
      rel="stylesheet"
    >
</head>
<body class="bg-light">

    <div class="container mt-5">
        <div class="card shadow-lg">
            <div class="card-header bg-primary text-white">
                <h3 class="text-center mb-0">Add New Farm</h3>
            </div>

            <div class="card-body">
                <form action="/farms" method="POST">

                    <!-- Farm Name -->
                    <div class="mb-3">
                        <label for="farmName" class="form-label">Farm Name</label>
                        <input 
                            type="text" 
                            id="farmName"
                            name="name" 
                            class="form-control" 
                            placeholder="Enter farm name" 
                            required
                        >
                    </div>

                    <!-- City -->
                    <div class="mb-3">
                        <label for="farmCity" class="form-label">City</label>
                        <input 
                            type="text" 
                            id="farmCity"
                            name="city" 
                            class="form-control" 
                            placeholder="Enter city name"
                        >
                    </div>

                    <!-- Email -->
                    <div class="mb-3">
                        <label for="farmEmail" class="form-label">Email</label>
                        <input 
                            type="email" 
                            id="farmEmail"
                            name="email" 
                            class="form-control" 
                            placeholder="Enter email address"
                        >
                    </div>

                    <!-- Submit Button -->
                    <button class="btn btn-primary w-100" type="submit">
                        Add Farm
                    </button>

                </form>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script 
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js">
    </script>
</body>
</html>
